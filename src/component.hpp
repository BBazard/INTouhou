#pragma once

typedef enum {
  UP,
  DOWN,
  LEFT,
  RIGHT,
  NONE,
} Direction;

typedef enum {
  PLAYER,
  ENEMY,
  NOTARGET,
} TargetType;

typedef enum {
  PLAYERSHIP,
  ENEMYSHIP,
  BULLET,
  BOMB,
  NOTDEFINE,
} EntityType;

class Component {
 public:
 private:
};

